
{
  "name": "json-buddy-app",
  "version": "1.0.0",
  "description": "JSON Buddy: Online JSON Formatter, Validator & Cleaner",
  "private": true,
  "scripts": {
    "dev": "echo \"Run index.html in a live server for frontend development. Vercel dev for serverless functions.\"",
    "build": "echo \"No explicit frontend build step required for this setup. Vercel handles API functions.\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
